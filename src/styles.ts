import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    alert: {

    },

    paper: {
        padding: 50,
        margin: 50
    },

    formInput: {
        marginBottom: 30
    }
});